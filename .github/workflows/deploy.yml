name: 部署到GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 下载代码
        uses: actions/checkout@v4
        
      - name: 安装Node.js v22
        uses: actions/setup-node@v4
        with:
          node-version: '22'  # 改为v22
          cache: 'npm'
          
      - name: 安装依赖
        run: |
          echo "📦 使用Node.js $(node --version)"
          echo "📦 使用npm $(npm --version)"
          
          rm -rf node_modules
          npm install
          
      - name: 构建网站
        run: npm run docs:build
        
      - name: 部署
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/.vitepress/dist