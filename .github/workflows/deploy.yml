name: 部署到GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      # 1. 下载代码
      - name: 下载代码
        uses: actions/checkout@v4
        
      # 2. 安装Node.js
      - name: 安装Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          
      # 3. 清理并重新安装依赖（修正版）
      - name: 清理并安装依赖
        run: |
          echo "🧹 开始清理依赖..."
          
          # 检查是否有 package-lock.json
          if [ -f "package-lock.json" ]; then
            echo "📄 找到 package-lock.json，使用 npm ci"
            rm -rf node_modules
            npm ci
          else
            echo "📦 没有 package-lock.json，使用 npm install"
            rm -rf node_modules
            npm install
          fi
          
          echo "✅ 依赖安装完成"
          
      # 4. 构建网站
      - name: 构建网站
        run: npm run docs:build
        
      # 5. 部署到GitHub Pages
      - name: 部署
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/.vitepress/dist