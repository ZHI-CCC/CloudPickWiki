name: 部署到GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 下载代码
        uses: actions/checkout@v4
        
      - name: 安装Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          
      - name: 安装依赖并修复
        run: |
          echo "🔧 安装依赖..."
          
          # 强制清理
          rm -rf node_modules
          
          # 检查package.json中vitepress版本
          if grep -q '"vitepress":' package.json; then
            echo "📦 检查到VitePress依赖"
          else
            echo "⚠️  未找到VitePress依赖，添加默认版本"
            npm install vitepress@1.6.3 --save-dev
          fi
          
          # 安装所有依赖
          npm install
          
          # 检查VitePress模块
          echo "🔍 检查VitePress文件..."
          ls -la node_modules/vitepress/dist/client/theme-default/ || echo "目录不存在"
          
      - name: 构建网站
        run: |
          # 先检查关键文件是否存在
          if [ -f "node_modules/vitepress/dist/client/theme-default/without-fonts" ] || 
             [ -f "node_modules/vitepress/dist/client/theme-default/without-fonts.js" ] ||
             [ -f "node_modules/vitepress/dist/client/theme-default/without-fonts.mjs" ]; then
            echo "✅ 找到without-fonts文件"
          else
            echo "⚠️  未找到without-fonts文件，尝试修复..."
            # 尝试重新安装vitepress
            npm install vitepress@1.6.3 --no-save
          fi
          
          # 执行构建
          npm run docs:build
          
      - name: 部署
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/.vitepress/dist