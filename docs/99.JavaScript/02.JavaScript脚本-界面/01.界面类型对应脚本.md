---
title: 界面类型对应脚本
date: 2099-03-05 21:25:02
permalink: /js/界面类型对应脚本
categories:
  - 脚本
tags:
  - 脚本
---
# 界面类型对应脚本
这里有关于界面类型的函数和方法

## 🤖 API 详解与脚本示例
### Segment 片段
⚠️ 
    注意 segment 是界面类型名！
```javascript
segment.isAdded(); // segment是否被添加
segment.isDetached(); // segment是否被卸载
segment.isHidden(); // segment是否被添加
segment.isVisible(); // segment是否可视
segment.show();  // 显示当前segment 只是调整可视状态
segment.hide();  // 隐藏当前segment 只是调整可视状态
segment.add('xx.yml'); // 当前片段基础上添加新的片段页面
segment.remove('xx.yml'); // 删除指定片段
segment.replace('xx.yml'); // 替换当前片段为指定片段
segment.close(); // 关闭调用方片段对象自身
segment.remove();  // 等价close
```
### Popup 弹出框
⚠️ 
    注意 popup 是界面类型名！
```javascript
popup.isShowing();  // 当前popup是否显示
popup.rootView = xx;  // 给popup设置view
```
