---
title: 内置函数
date: 2099-03-05 21:25:02
permalink: /js/内置函数
categories:
  - 脚本
tags:
  - 脚本
---
# 内置函数

## 🔧 JSON转换

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) | 返回值 (Return Value) |
| :--- | :--- | :--- | :--- |
| **`JSON.parse(jsonString)`** | 将JSON格式的字符串内容转为JavaScript对象。 | `string` | `object` |
| **`JSON.stringify(object)`** | 把JavaScript对象或数组转换成JSON字符串。 | `object` / `array` | `string` |
| **`Java.asJSONCompatible(object)`** | 将JavaScript对象或数组转为Java可以直接使用的集合类型。 | `object` / `array` | Java Collection |

## 🔧 Basic 基本转换

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) | 返回值 (Return Value) | 备注 (Notes) |
| :--- | :--- | :--- | :--- | :--- |
| **`str(value)`** | 将任意类型转为文本。 | `any` | `string` | 转换失败返回 "null" 字符串。 |
| **`int(value)`** | 将值转换为整数。 | `any` | `int` | 转换失败返回 `-1`。 |
| **`double(value)`** | 将值转换为双精度浮点数。 | `any` | `double` | 转换失败返回 `-1`。 |
| **`float(value)`** | 将值转换为单精度浮点数。 | `any` | `float` | 转换失败返回 `-1`。 |
| **`bool(value)`** | 将值转换为布尔值。 | `any` | `boolean` | 转换失败返回 `false`。 |

## 👻 Entity 实体

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`Entity.getPointerName()`** | 获取鼠标指针悬停实体的名字。 | `string` |
| **`Entity.getPointerUUID()`** | 获取鼠标指针悬停实体的UUID文本。 | `string` |
| **`Entity.getPointerHealth()`** | 获取鼠标指针悬停实体的当前生命值。 | `float` |
| **`Entity.getPointerMaxHealth()`** | 获取鼠标指针悬停实体的最大生命值。 | `float` |

## ⏰ Dispatcher 调度器

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) |
| :--- | :--- | :--- |
| **`Dispatcher.post(function)`** <br> `调度器.执行(function)` | 异步执行某个函数。 | `function` |
| **`Dispatcher.postDelayed(function, delay)`** <br> `调度器.延迟执行(function, delay)` | 异步延迟指定毫秒数后执行某个函数。 | `function`, `long` (毫秒) |
| **`Dispatcher.postAtTime(function, time)`** <br> `调度器.指定时间执行(function, time)`| 在指定的时间点异步执行某个函数。 | `function`, `long` (时间戳) |
| **`Dispatcher.remove(function)`** <br> `调度器.取消(function)` | 取消一个正在等待执行或循环执行的函数。 | `function` |

## 📊 Placeholder 变量

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) | 返回值 (Return Value) |
| :--- | :--- | :--- | :--- |
| **`Placeholder.set(key, value)`** <br> `变量.设置(key, value)` | 创建或更改客户端内已缓存的PAPI变量值。 | `string`, `string` | `void` |
| **`Placeholder.get(key)`** <br> `变量.获取(key)` | 获取当前客户端内的缓存变量值。 | `string` | `object` |
| **`Placeholder.update(key1, ...)`** <br> `变量.更新(key1, ...)` | 发包请求服务端更新一个或多个PAPI变量值。 | `string...` | `void` |

## 💎 Item 物品

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) | 返回值 (Return Value) |
| :--- | :--- | :--- | :--- |
| **`Item.getMainHandItem()`** | 获取玩家主手上的物品。 | `void` | `ItemStack` |
| **`Item.getOffHandItem()`** | 获取玩家副手上的物品。 | `void` | `ItemStack` |
| **`Item.getArmorValue(itemStack)`** | 获取指定ItemStack对象的护甲值。 | `ItemStack` | `int` |
| **`Item.getHoverItem()`** | 获取当前鼠标悬浮的物品。 | `void` | `ItemStack` |
| **`Item.getCount(itemStack)`** | 获取指定ItemStack对象的数量。 | `ItemStack` | `int` |
| **`Item.getName(itemStack)`** | 获取指定ItemStack对象的名字。 | `ItemStack` | `string` |
| **`Item.getLoreCount(itemStack)`** | 获取指定ItemStack对象的Lore行数。 | `ItemStack` | `int` |
| **`Item.getLore(itemStack, index)`** | 获取指定ItemStack对象的某一行Lore。 | `ItemStack`, `int` | `string` |
| **`Item.getFormattedLore(itemStack, index)`**| 获取带格式的Lore行，可直接用于TextBlock。| `ItemStack`, `int` | `FormattedTextBuilder` |
| **`Item.getLores(itemStack)`** | 获取指定ItemStack对象的全部Lore。 | `ItemStack` | `List<string>` |
| **`Item.serialize(itemStack)`** | 获取指定ItemStack对象的NBT，并序列化为JSON文本。| `ItemStack` | `string` |
| **`Item.deserialize(json)`** | 根据JSON文本反序列化为ItemStack对象。 | `string` | `ItemStack` |
| **`ltem.getComponentLore(itemStack)`** | 直接获取Component的实例。| `ItemStack` | `Component` |

## 🖥️ Screen 屏幕

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) |
| :--- | :--- | :--- |
| **`Screen.open(guiName)`** | 打开对应的GUI。 | `string` |
| **`Screen.closeCurrent()`** | 关闭当前的GUI。 | `void` |
| **`Screen.getCurrentTitle()`** | 获取当前打开的GUI标题。 | `void` |
| **`Screen.returnToGame()`** | 返回游戏（关闭当前界面）。 | `void` |
| **`Screen.openAdvancementsScreen()`** | 打开进度界面。 | `void` |
| **`Screen.openStatsScreen()`** | 打开统计界面。 | `void` |
| **`Screen.openOptionsScreen()`** | 打开选项界面。 | `void` |
| **`Screen.disconnectGame()`** | 断开服务器连接。 | `void` |
| **`Screen.exitToTitleScreen()`** | 退出到游戏主标题界面。 | `void` |
| **`Screen.getLevelLoadingProgress()`** | 获取地图加载界面的进度。 | `void` |

## 🖱️ Mouse 鼠标

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`Mouse.getX()`** | 获取当前鼠标X坐标。 | `double` |
| **`Mouse.getY()`** | 获取当前鼠标Y坐标。 | `double` |
| **`Mouse.getXVelocity()`** | 获取当前鼠标X速度。 | `double` |
| **`Mouse.getYVelocity()`** | 获取当前鼠标Y速度。 | `double` |
| **`Mouse.grab()`** | 抓取鼠标（锁定在窗口内）。 | `void` |
| **`Mouse.release()`** | 释放鼠标。 | `void` |

## 💬 Chat 聊天

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Chat.send(message)`** | 以玩家身份发送一条聊天消息。 |
| **`Chat.sendSelf(message)`** | 仅向玩家自己发送一条客户端消息。 |

## 🔥 Furnace 熔炉

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`Furnace.getBurnProgress()`** | 获取当前熔炉的燃烧进度。 | `int` (-1 if failed) |
| **`Furnace.getLitProgress()`** | 获取当前熔炉的烧炼进度。 | `int` (-1 if failed) |

## ⌨️ Input 输入

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) | 返回值 (Return Value) |
| :--- | :--- | :--- | :--- |
| **`Input.isKeyDown(keyName)`** | 判断指定的按键当前是否被按下。 | `string` | `boolean` |

## 📡 Packet 数据包

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Packet.sendData(identifier, ...)`** | 向服务端发送自定义数据包。 |
| **`Packet.sendKey(keyId)`** | 执行`KeyConfig.yml`里定义的按键指令。 |

## ✨ Animation 动画

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Animation.play(view, animationName)`** | 为一个控件对象播放指定名称的动画。 |
| **`Animation.stop(view, animationName)`** | 停止控件对象上正在播放的指定动画。 |

## 💬 Dialog 对话框

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Dialog.show(fileName)`** | 打开一个对话框。 |
| **`Dialog.show(fileName, position)`** | 在屏幕指定相对位置打开一个对话框。 |

## 🪟 SystemDialog 系统对话框

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`SystemDialog.openDialogGet()`** | 打开系统文件路径选择器。 | `string` |
| **`SystemDialog.messageBox(text)`** | 打开一个系统消息框。 | `void` |

##  HUD 全天候显示

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Hud.setVisibility(hudName, visible)`** | 设置原生HUD元素的可见性（如`PlayerHealth`）。 |

## 📋 Clipboard 剪切板

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`Clipboard.getText()`** | 获取系统剪切板的文本内容。 | `string` |
| **`Clipboard.setText(text)`** | 设置系统剪切板的文本内容。 | `void` |

## 👤 Player 玩家

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`Player.getHandSlotIndex()`** | 获取玩家当前手持物品的索引。 | `int` |
| **`Player.getArmorValue()`** | 获取玩家的总护甲值。 | `int` |
| **`Player.getOxygen()`** / **`getMaxOxygen()`** | 获取玩家当前/最大氧气值。 | `int` |
| **`Player.dropHandItem()`** | 丢弃手上物品。 | `void` |
| **`Player.getName()`** | 获取玩家的名称。 | `string` |
| **`Player.getHealth()`** / **`getMaxHealth()`** | 获取玩家当前/最大生命值。 | `float` |
| **`Player.getExperience()`** | 获取玩家当前经验值。 | `float` |
| **`Player.getLevel()`** | 获取玩家的等级。 | `float` |
| **`Player.getPosX()`** / **`getPosY()`** / **`getPosZ()`** | 获取玩家的世界坐标。 | `double` |
| **`Player.getBlockX()`** / **`getBlockY()`** / **`getBlockZ()`** | 获取玩家脚下方块的世界坐标。 | `double` |
| **`Player.getUUID()`** | 获取玩家的UUID文本。 | `string` |
| **`Player.isFly()`** | 判断玩家是否在飞行。 | `boolean` |
| **`Player.getHunger()`** | 获取玩家的饥饿度。 | `float` |

## 📨 Message 消息

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Message.self(text)`** | 使玩家向自己发送一条客户端消息。 |
| **`Message.broadcast(text)`**| 使玩家发送一条广播消息（通常需要权限）。 |
| **`Message.title(text)`** | 以Title形式显示一条消息。 |

## 🌈 RichText 富文本

| 方法 (Method) | 说明 (Description) | 返回值 (Return Value) |
| :--- | :--- | :--- |
| **`RichText.legacyToFormattedTextBuilder(text)`** | 将带样式符号的字符串转为可直接用于UI的`FormattedTextBuilder`对象。 | `FormattedTextBuilder` |
| **`RichText.legacyToPlain(text)`** | 将带样式符号的字符串转为纯文本。 | `string` |

## 🛠️ Util 工具

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Util.takeScreenshot()`** | 截图当前游戏界面并保存。 |
| **`Util.openUrl(url)`** | 在浏览器中打开一个链接。 |
| **`Util.openEditor()`** | 打开本地桌面编辑器。 |

## 🔊 Audio 音频

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Audio.play(fileName)`** | 播放客户端资源包内的音频文件。 |
| **`Audio.stop(fileName)`** | 停止播放指定的音频文件。 |

## 📦 Slot 槽位

| 方法 (Method) | 说明 (Description) | 参数 (Parameters) |
| :--- | :--- | :--- |
| **`Slot.performClick(slotName, button, type)`** | 模拟对指定槽位的点击操作。 | `string`, `int` (0左,1右,2中), `string` (点击类型) PICKUP,QUICK_MOVE,SWAP,CLONE,THROW,QUICK_CRAFT,PICKUP_ALL(不写则为PICKUP)|
| **`Slot.getInventoryItem(slotName)`** | 获取玩家**背包**中指定槽位的物品。 | `string` |
| **`Slot.getContainerItem(slotName)`** | 获取当前打开的**容器**（如箱子）中指定槽位的物品。 | `string` |

## ⏏️ Popup 弹出框

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Popup.show(view, fileName)`** | 依附于指定控件打开一个弹出框。 |
| **`Popup.close(view, fileName)`** | 关闭依附于指定控件的弹出框。 |
| **`Popup.show(view, fileName, x, y)`**| 在相对于控件的指定偏移位置打开弹出框。 |

## 🏤 Gloabl 全局变量

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Global.put('xxx','a');`** | 创建一个名为xxx的全局变量，内容是a。 |
| **`Global.xxx`** | 获取名为xxx的全局变量信息。 |

## 🐞 Debug 调试

| 方法 (Method) | 说明 (Description) |
| :--- | :--- |
| **`Debug.print(content)`** | 在客户端日志中输出调试内容。 |